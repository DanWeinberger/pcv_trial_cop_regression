# Raw WISSPAR export snapshot (~4 MB). Re-fetched on demand by
# R/build_wisspar_head2head.R; only the small filtered slices are committed.
data/wisspar_export.json

# R session cruft
.Rhistory
.RData
.Rproj.user/
data/run_out.log

# Model outputs -- fully regenerable via R/run_models.R and R/compare_waic.R
results/
